# Архивы для заливки на хостинг собираются из DearBoxV1 — в истории не нужны.
*.zip

# Настройки локального просмотра и служебные файлы Claude — к сайту не относятся.
.claude/

# Тексты заданий для Claude — рабочие заметки, на GitHub не нужны.
prompt-*.md

# Инструменты сборки — ставятся командой npm install, в историю не нужны.
node_modules/

# Данные админки при локальной проверке (на сервере — рядом с httpdocs).
# Пароль, сессии, отметки бариста — в историю не должны попасть никогда.
dearbox-data/

# Черновики профиля GitHub от Claude в Cowork — к сайту не относятся.
Claude outputs/
